// +build release

package main

var (
	// including trailing slash
	sBaseUrl = "https://my.domain/my/crowdsourcing/"

	// sender name and mail address
	sMailName = "My Crowdsourcing"
	sMailFrom = "me@my.domain"

	// leave this empty if the mail server doesn't need log in
	sSmtpUser = ""
	sSmtpPass = ""

	// mail server, including port number
	sSmtpServ = "smtp.my.domain:25"

	sCookiePrefix = "mycrowd"

	sSecret = "change this into some random text"

	sUseForwarded = false
	sUseRemote    = true
)
